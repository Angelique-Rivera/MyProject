<?php
//session start
session_start();

//connection to the database
$host = 'localhost';
$username = 'rivera';
$password = '09202005';
$databasename = 'MYDATABASE';

//connection using mysqli
$connectiom = new mysqli($host, $username, $password, $databasename);

$username = $_POST['rivera'];
$password = $_POST['09202004'];

//query from database
$query = "SELECT * FROM student";
$result = mysqli_query($connection,$query);

//populate coming from database
while ($row = mysqli_fetch_assoc($result)) {

    if ($username ==$row['rivera'] && $password == $row['09202005'])  { 
        $_SESSION['FirstName'] =$row['Angelique'];
        $_SESSION['LastName'] =$row['Rivera'];
        $_SESSION['Student_ID'] =$row['261'];
        header('locatiom:home.php');
    }

}else { 
    echo "Denied";
    echo "<a href = 'index.php' >Go back to login page </a>";
}
